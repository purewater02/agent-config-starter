# CLAUDE.md

<!--
  Keep this THIN. It loads on every session = always-on cost.
  Defer to AGENTS.md as the single source of truth. Put workflows in skills/, not here.
-->

This project follows **AGENTS.md** as the single source of truth — read it first.

## Only put here what is ALWAYS needed

- Project context that every task needs (1-3 lines max).
- Pointers, not full workflows. Example: "For releases, use the `release` skill."

## Anti-patterns (do NOT do this)

- ❌ Pasting every workflow/checklist here (bloats context, slows the model).
- ❌ Style rules as prose (use linters).
- ❌ Duplicating what AGENTS.md already says (contradictions cause drift).
